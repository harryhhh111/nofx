# AI 计算卸载与 Prompt 精简方案

## 主旨

将机械计算从 AI（DeepSeek Flash/Pro）移到 Go 代码，精简 prompt，让 AI 专注策略判断。

核心原则：**代码计算，AI 决策。不给 AI 它能算错的东西，不给 AI 代码已经知道答案的东西。**

---

## 一、摘要增强（不再需要原始数组的前提）

### 1.1 目前摘要已有的内容

**Compact Kline（`formatCompactKlines`）**：
- 当前 K 线：OHLC、实体、影线、阴阳
- 前 3 根序列 + 高低点趋势（抬高/降低/分化）
- 量能：当前 vs 20均 + 百分比 + 5根趋势
- 波动：5根区间 + 全窗口区间
- 关键位：摆动高低点（前高/前低 + 根数）
- 区间位置百分比

**指标摘要（`formatTimeframeSummary`）**：
- EMA：多空排列、方向箭头、开口趋势（扩大/收敛/稳定）
- ADX：强度分级（弱/中等/强/极强）、趋势方向、DI 方向
- BOLL：价格位置（超买/偏多/偏空/超卖）、带宽趋势
- ATR：数值、价格占比、波动分级
- SAR：方向、价格距离、翻转检测、持续周期
- RSI：数值+趋势箭头、区间（超卖/偏弱/偏强/超买）

### 1.2 需要新增的摘要字段

#### A. 市场状态标签（加到指标摘要首行）

根据 ADX 值自动判定，替代 AI 手动比对阈值：

```
ADX < 20  → "震荡市(均值回归策略)"
ADX 20-25 → "过渡市(谨慎交易)"
ADX > 25  → "趋势市(趋势跟踪策略)"
```

输出示例：`市场状态: 趋势市(ADX=37.94 中等↑, 多方主导)`

**实现位置**：`formatTimeframeSummary` ADX 部分，在现有输出前插入一行。

#### B. RSI 极端值护栏

根据 RSI7/RSI14 判定极端区域，直接给出做多/做空是否安全：

```go
// 做多禁止条件：RSI7 >= 80 OR RSI14 >= 70
// 做空禁止条件：RSI7 <= 20 OR RSI14 <= 30
longBlocked := rsi7 >= 80 || rsi14 >= 70
shortBlocked := rsi7 <= 20 || rsi14 <= 30
```

输出示例：`RSI: 7=67.76↑ 14=59.99↑, 偏强, 做多安全, 做空允许`
或：`RSI: 7=82.50↑ 14=72.30↑, 超买, 做多禁止(超买), 做空允许`

**实现位置**：`formatTimeframeSummary` RSI 部分。

#### C. RSI 背离检测

检测最近 N 根 K 线内价格与 RSI14 的背离：

```
检测窗口：最近 5 根（15m/1h）或 7 根（5m）
顶背离：价格创窗口内新高但 RSI14 未创窗口内新高 → "顶背离(看跌)"
底背离：价格创窗口内新低但 RSI14 未创窗口内新低 → "底背离(看涨)"
```

输出示例：`[RSI底背离: 价格新低但RSI未确认]` 或 `[无背离]`

**实现位置**：`formatTimeframeSummary` RSI 部分末尾追加。

#### D. EMA 交叉信号

检测最近 3 根内是否发生金叉/死叉：

```go
// 金叉：前一根 EMA20 <= EMA50 且 当前 EMA20 > EMA50
// 死叉：前一根 EMA20 >= EMA50 且 当前 EMA20 < EMA50
```

输出示例：`EMA: 多头排列... [1根前金叉]` 或 `EMA: 多头排列... [无交叉]`

**实现位置**：`formatTimeframeSummary` EMA 部分末尾追加。

#### E. 成交量极端标注

在 compact kline 中标注极端放量 K 线：

```go
// 如果某根K线成交量 > 近20均 × 3.0 → 标注"第N根放量X倍"
// 如果某根K线成交量 > 近20均 × 5.0 → 标注"第N根巨量X倍"
```

输出示例：`量: 当前85.2 vs 近20均120.5(缩量29%), 近5根量能递减, [第3根放量3.5倍]`

**实现位置**：`formatCompactKlines` 成交量部分末尾追加。

#### F. OI 资金流向解读（币种级）

根据 OI 变化 + 价格变化，自动输出解读：

```go
oiUp := oi.Latest > oi.Average
priceUp := data.CurrentPrice > data.PrevPrice  // 需要 1h 前价格

// 对照表：
// OI↑+价↑ → 多头主导(资金流入做多)
// OI↑+价↓ → 空头主导(资金流入做空)
// OI↓+价↑ → 空头平仓(可能出现反转)
// OI↓+价↓ → 多头平仓(可能出现反转)
```

输出示例：`OI: 100332(↑+0.1%) + 价格↑ → 多头主导(资金流入)`

**实现位置**：`formatMarketDataFromSnapshot` OI 部分。

### 1.3 增强后的摘要示例（15m 完整输出）

```
K线摘要(15m, 最近20根):
  当前: O=62720.50 H=62799.90 L=62620.00 C=62743.70, 实体23点(阳线), 上影56点, 下影124点
  前3根: 阴(实体85点) → 阳(实体40点) → 阳(实体23点), 高低点抬高
  量: 当前92.3 vs 近20均110.5(缩量16%), 近5根量能递减, [第4根放量3.2倍]
  波动: 近5根区间62580-62790(210点), 近20根区间62200-63200(1000点)
  关键位(摆动高低点):
    上方阻力: 63200(前高, 15根前), 62950(前高, 8根前)
    下方支撑: 62580(前低, 2根前), 62300(前低, 10根前)
  区间位置: 54% (当前价在近20根区间中性位)

市场状态: 趋势市(ADX=37.94 中等↑, 多方主导)
EMA: 多头排列 (20=62627.10↑ > 50=62456.34↑), 开口扩大 [无交叉]
RSI: 7=67.76↑ 14=59.99↑, 偏强, 做多安全, 做空允许 [无背离]
BOLL: 价格 62743.70 中轨上方(偏多), 中轨 62647.04, 带宽 2.3% 收窄中
ATR14: 198.51 (占价格0.32%), 正常波动
SAR: 62350.00 上行, 价格距SAR +0.6%(393点), 趋势持续8周期 [无翻转]
```

---

## 二、用户 Prompt 增加快速参考

在每个币种的市场数据块末尾，增加一行预计算参考值：

```
---
快速参考: ATR14(15m)=198.51, SL缓冲需求≥198.51点 | 
  仓位上限(权益=10198): 高信心≤8158, 中信心≤5099, 低信心≤3059 USDT
```

**实现位置**：`formatMarketDataFromSnapshot` 末尾，或 `BuildUserPrompt` 中。

计算公式：
```go
atr14 := data.TimeframeData["15m"].ATR14
maxPos := accountEquity * posRatio  // posRatio: btc/eth=5.0, alt=1.0

highConf := maxPos * 0.8    // 高信心(≥85): 80%
medConf  := maxPos * 0.5    // 中信心(70-84): 50%
lowConf  := maxPos * 0.3    // 低信心(60-69): 30%
```

---

## 三、系统 Prompt 精简

### 3.1 可以删除/大幅缩短的段落

#### 当前 prompt 第 38-50 行：OI 变化解读表

```
当前（~350 字符）:
## 💹 持仓量(OI)变化解读
- **OI增加 + 价格上涨**: 强多头趋势...
- **OI增加 + 价格下跌**: 强空头趋势...
- **OI减少 + 价格上涨**: 空头平仓...
- **OI减少 + 价格下跌**: 多头平仓...

改为（~80 字符）:
## 💹 OI解读
每个币种数据中已包含 OI 资金流向预判，直接引用即可。
```

因为每个币种的数据块已经预计算了"多头主导/空头主导/空头平仓/多头平仓"。

#### 当前 prompt 第 63-74 行：止损与入场质量

```
当前（~650 字符）:
- **重要：使用 15m 或 1h ATR14 评估止损...
- 止损缓冲：止损设在支撑/阻力位之外至少 1.0 × ATR14...
  公式（做多）：止损 = 支撑 - ATR14(15m) × 1.0
- 入场质量：仅在 ① 关键支撑/阻力位附近...
- 若止损距离（入场到止损）< 1.0 × ATR14...

改为（~200 字符）:
- SL缓冲≥快速参考中的ATR14值（入场到止损距离必须≥此值）
- 仅在 ① 关键支撑/阻力位附近（反转）或 ② 突破确认后（趋势）入场
- 若SL距离<ATR缓冲→跳过或等待更好入场点
```

因为 ATR14 缓冲值已在快速参考中给出，不需要 AI 自己算。

#### 当前 prompt 第 76-95 行：开仓决策流程

```
当前（~1000 字符）包含完整的 R:R 手工计算步骤:
第一步：先确定技术位...
  设 止损 = 支撑 - ATR 缓冲...
  设 止盈 = 图表结构中的目标位
  计算 R:R 比率 = |止盈 - 入场| / |入场 - 止损|
  若 R:R < 1.5 → 跳过该交易

改为（~350 字符）:
第一步：确定技术位
  - 在你的分析时间框架上找到支撑/阻力
  - SL = 支撑 - ATR缓冲（做多）或 阻力 + ATR缓冲（做空）
  - TP = 图表结构中的真实目标位
  - R:R ≥ 1.5，不要缩小止损来凑比率

第二步：根据信心确定仓位大小
  - 高信心(≥85): ≤快速参考值 / 中信心(70-84): ≤快速参考值 / 低信心(60-69): ≤快速参考值
```

删除"计算 R:R 比率"的公式说明，删除"禁止缩小止损来减少潜在亏损"的重复警告（后面已有）。

#### 当前 prompt 第 104-138 行：入场标准

```
当前趋势市入场条件包含:
5. 止损合理：基于 ATR14 缓冲（阻力位 + 1.0×ATR）
6. 盈亏比 ≥ 1.5：止盈位来自真实图表结构

这两项在"开仓决策流程"已经详细说明了，可以精简为:
5. 止损基于ATR缓冲，盈亏比≥1.5（详见决策流程）
```

节省有限，可以不删。但把 ATR 缓冲的具体数值引用改为"见快速参考"。

### 3.2 不需要改的部分

以下段落保留不变，它们是纯策略逻辑，无法由代码替代：

- **趋势市入场条件**（ADX>25）的 EMA/SAR/布林带条件——纯策略判断
- **震荡市入场条件**（ADX<20）的 RSI/布林带/K线形态条件——纯策略判断
- **基于论点的持仓管理**——纯策略逻辑
- **强制平仓检查清单**（6 项）——需要 AI 综合判断
- **输出格式说明**——必须保留

### 3.3 预估节省

| 段落 | 当前字符数 | 精简后 | 节省 |
|------|----------|--------|------|
| OI 解读表 | ~350 | ~80 | ~270 |
| 止损与入场质量 | ~650 | ~200 | ~450 |
| 开仓决策流程 | ~1000 | ~350 | ~650 |
| **系统 prompt 合计** | | | **~1370 字符** |

系统 prompt 从约 8000 字符降到约 6600 字符（~17%）。

---

## 四、原始数组移除

### 4.1 当前行为 vs 目标行为

```
当前（formatTimeframeSeriesData）:
  摘要模式: 摘要 + 原始数组（如果指标不在摘要白名单）
  非摘要模式: 完整 OHLCV 表 + 全部原始数组

目标:
  摘要模式: 仅摘要（无原始数组，无原始 OHLCV 表）
  非摘要模式: 保持原有行为（供需要原始数据的场景使用）
```

### 4.2 代码改动

`kernel/engine_prompt.go` `formatTimeframeSeriesData`：

```go
// Indicator section
if indicators.IsTimeframeSummarized(timeframe) {
    e.formatTimeframeSummary(sb, data, indicators)
    // 删除：不再输出未摘要指标的原始数组 fallback
    // （1834-1858 行全部删除）
} else {
    // 非摘要模式保持不变
    ...
}
```

如果担心某些指标需要保留 raw 数组作为 backup，可以加一个全局开关 `KeepRawArraysOnSummary bool`，默认 `false`。

### 4.3 预估节省

以 t5-2 为例，3 个币种 × 15m 原始数组：

| 指标 | 每数组约 | 数量 | 小计 |
|------|---------|------|------|
| EMA20/EMA50 | 300 字符 | 2 | 600 |
| ADX/+DI/-DI | 300 字符 | 3 | 900 |
| RSI7/RSI14 | 250 字符 | 2 | 500 |
| BOLL Upper/Mid/Lower | 300 字符 | 3 | 900 |
| SAR | 300 字符 | 1 | 300 |
| Volume | 180 字符 | 1 | 180 |
| MACD | 300 字符 | 1 | 300 |
| **单币种单周期** | | | **~3680 字符** |
| **3 币种** | | | **~11000 字符** |

加上完整 OHLCV 表（20 行 × 60 字符 = 1200/币种），每个币种约 5000 字符。

**3 币种总计节省 ~15000 字符**（约 4000-5000 tokens）。

---

## 五、实现优先级

### 第一批（核心，建议先做）

1. **原始数组移除** — 删掉 1834-1858 行的 raw fallback
2. **市场状态标签** — 在 ADX 摘要前插入 regime 行
3. **RSI 极端值护栏** — 在 RSI 摘要中加 `做多安全/禁止` `做空安全/禁止`
4. **系统 prompt OI 解读表删除** — 改为引用数据中的预判

### 第二批（增强质量）

5. **RSI 背离检测** — 新增计算逻辑
6. **EMA 交叉检测** — 新增计算逻辑
7. **成交量极端标注** — compact kline 中标注极端量能
8. **用户 prompt 快速参考** — ATR 缓冲值 + 仓位档位

### 第三批（锦上添花）

9. **OI 资金流向预判** — 币种级 OI+价格解读
10. **系统 prompt 开仓决策流程精简** — 删除 R:R 手工计算公式

---

## 六、不需要改动的文件

- `store/strategy.go` — 配置结构无需变化（不需要新字段）
- `market/types.go` — 数据结构无需变化
- 前端 — 不需要 UI 改动

所有改动集中在：
- `kernel/engine_prompt.go` — 摘要增强 + 原始数组移除 + 快速参考
- 数据库 `prompt_sections` — 系统 prompt 文本精简

---

## 七、验证方法

1. `go build -o nofx .` 确保编译通过
2. 用 t5-2 的配置跑一个决策周期，对比：
   - prompt 长度变化（预期减少 40-50%）
   - AI 思维链是否还包含手工数值计算
   - 决策质量是否受影响
3. 检查 enhance 后的摘要是否有计算错误（边界情况：数据不足时 fallback 是否优雅）
